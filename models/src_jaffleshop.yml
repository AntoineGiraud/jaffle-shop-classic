sources:
  - name: stripe
    description: stripe data loaded by Ingestion Team
    database: demo_dbt_jaffleshop
    schema: raw_stripe
    tables:
      - name: raw_payment
  - name: jaffle
    description: Jaffle Shop data loaded by Ingestion Team
    database: demo_dbt_jaffleshop
    schema: raw_jaffle_shop
    tables:
      - name: raw_orders
      - name: raw_customers
